# Backend API base URL. If you provide only the domain, /api will be appended automatically.
VITE_API_BASE_URL=https://your-api.onrender.com/api

# Optional test-only role switch flag.
VITE_ENABLE_TESTING_ROLE_SWITCH=false
